# Google Apps Script Web App URL
# 部署 Apps Script 后获得的 URL (以 /exec 结尾)
VITE_GOOGLE_APP_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# 通过阈值 (答对几题算通过)
VITE_PASS_THRESHOLD=3

# 每次游戏抽取的题目数量
VITE_QUESTION_COUNT=5

# 是否使用本地模拟数据 (true/false)
# 设置为 true 时不连接 Google Sheets，直接使用本地 Mock 数据
# 设置为 false 时连接 Google Sheets
VITE_USE_MOCK=false
